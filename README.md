# url-vir

Url helpers. In particular, the function `buildUrl` provides an easy way to combine multiple parts of a URL together.

Docs: https://electrovir.github.io/url-vir

## Install

```sh
npm i url-vir
```
